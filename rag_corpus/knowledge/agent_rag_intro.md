# 企业信用 Agent 知识库示例

本目录用于本地 RAG 检索，默认由 `search_knowledge_base` 工具读取。

## 事实型约束

1. 企业信用相关结论应尽量基于工具返回数据与可追溯证据，不应凭空编造。
2. 若证据不足，优先继续检索或向用户追问最小必要信息。
3. 返回结果建议包含：结论、依据、建议。

## 授权流程提示

当工具返回 `AUTH_REQUIRED` 时，系统应引导用户先完成授权，再继续后续分析。

