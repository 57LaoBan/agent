# 当前进度

## 已完成

- 初始化独立 Agent 目录：`D:\companyCode\agent`。
- 建立测试分层：`smoke`、`contract`、`e2e`。
- 跑通阿里云百炼 `qwen-plus` OpenAI 兼容接口冒烟测试。
- 建立第一版单 Agent 受控 loop 骨架。
- 将请求、响应、trace 结构统一收敛到 `protocol.py`，避免和契约测试目录混名。
- 加入 FastAPI HTTP 入口，接口层不承载业务编排。
- 新增 `web/` 前端工作台，左侧聊天区只展示用户可见内容，右侧检测窗口展示意图、工具、状态和原始事件。
- 扩展 `AgentEvent`、`RouteDecision`、`ToolCall`、`ToolResult`、`PendingAction` 等协议，并将 runtime 升级为循环式事件生成器。
- 新增 `tools` 层，将 `rag_search` 和 mock 授信额度查询封装为工具，runtime 不再直接接收 retriever。
- 为工具增加分类、风险等级和工具描述，注册表可按分类暴露工具，并拦截当前路由不允许的工具调用。
- 新增 `router` 层，将意图识别拆为规则优先、模型结构化识别、策略校验三段，runtime 不再内置路由规则。
- 将业务槽位和工具输入输出契约绑定，注册表会强校验工具输入槽位和输出槽位的字段名与类型。
- 吸收旧版协议中的统一封装思想，新增工具结果 envelope、证据状态、模型决策、前端动作、性能摘要和协议版本字段。

## 当前设计取舍

- 主路径采用单 Agent 问答，不引入复杂 planner 或多 agent 协作。
- RAG 作为 `rag_search` 工具接入，模型调用、响应契约、诊断 trace 分模块维护。
- 路由阶段先决定允许的工具分类，再暴露该分类下的具体工具，减少模型误调用和越权调用。
- 意图识别阶段允许模型提出候选判断，但最终由 `RoutePolicy` 校验置信度、槽位、工具分类和风险等级。
- 槽位是业务字段契约，不只是提示模型的文本；工具缺少必填输入或输入类型错误会被 `blocked`，输出缺少必填字段或输出类型错误会被标记为 `failed`。
- 工具结果既保留当前 `ToolResult`，也提供 `ToolResultEnvelope`，用于后续前端动作、审计、错误码和跨工具返回格式统一。
- 右侧检测窗口后续直接消费结构化 trace，而不是解析聊天文本。
- 前端按 `visibility` 分流同一条 Agent 事件流，避免把诊断过程污染到聊天框。

## 下一步

- 接入真实 RAG 检索接口。
- 定义前端检测窗口需要的 `retrieval_trace`、`citation_trace`、`rule_trace`。
- 增加政策问答、产品推荐、准入规则判断三个端到端场景。
- 扩充意图识别评测样本，覆盖口语化、含糊表达、跨场景表达和恶意越权表达。
